@using Microsoft.AspNetCore.Identity
@using Ticketing.Core.Models
@inject UserManager<ApplicationUser> UserManager
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Ticketing Admin</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/flowbite/2.2.1/flowbite.min.css" rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
    
    <style>
        :root {
            --color-primary-base: #3b82f6;
            --color-primary-dark: #2563eb;
            --color-primary-light: #60a5fa;
            --color-sidebar: #e0f2fe;
        }

        body {
            font-family: 'Inter', sans-serif;
            background-color: #f0f9ff;
        }

        .btn-primary {
            background-color: var(--color-primary-base);
            color: white;
            transition: all 150ms ease-in-out;
        }

        .btn-primary:hover {
            background-color: var(--color-primary-dark);
        }

        .sidebar-gradient {
            background: linear-gradient(180deg, #dbeafe 0%, #bfdbfe 100%);
        }

        .active-sidebar-item {
            background-color: rgba(59, 130, 246, 0.15);
            color: #1e40af;
            border-left: 3px solid var(--color-primary-base);
        }

        .active-sidebar-item svg {
            color: #2563eb;
        }
    </style>
</head>
<body class="bg-sky-50">
    <!-- Sidebar -->
    <aside id="sidebar" class="fixed top-0 left-0 z-40 w-64 h-screen transition-transform -translate-x-full lg:translate-x-0" aria-label="Sidebar">
        <div class="h-full px-4 py-6 overflow-y-auto sidebar-gradient shadow-lg">
            <a asp-page="/Index" class="flex items-center mb-8">
                <div class="flex items-center space-x-3">
                    <div class="w-8 h-8 bg-gradient-to-br from-blue-500 to-blue-600 rounded-lg flex items-center justify-center shadow-md">
                        <svg class="w-4 h-4 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path>
                        </svg>
                    </div>
                    <span class="self-center text-xl font-bold whitespace-nowrap text-blue-900">Ticketing Admin</span>
                </div>
            </a>
            <div class="mb-6">
                <p class="text-xs font-semibold text-blue-700 uppercase tracking-wider mb-3 px-2">ACCUEIL</p>
                <ul class="space-y-1">
                    <li>
                <a href="/" class="sidebar-link flex items-center p-3 text-gray-700 rounded-lg hover:bg-blue-100/50 transition-all duration-200 group" data-page="/">
                            <svg class="w-5 h-5 text-gray-600 transition duration-200" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="currentColor" viewBox="0 0 22 21">
                                <path d="M16.975 11H10V4.025a1 1 0 0 0-1.066-.998 8.5 8.5 0 1 0 9.039 9.039.999.999 0 0 0-1-1.066h.002Z"/>
                                <path d="M12.5 0c-.157 0-.311.01-.565.027A1 1 0 0 0 11 1.02V10h8.975a1 1 0 0 0 1-.935c.013-.188.028-.374.028-.565A8.51 8.51 0 0 0 12.5 0Z"/>
                            </svg>
                            <span class="ml-3 font-medium">Tableau de bord</span>
                        </a>
                    </li>
                </ul>
            </div>
            <div>
                <p class="text-xs font-semibold text-blue-700 uppercase tracking-wider mb-3 px-2">PAGES</p>
                <ul class="space-y-1">
                    @if (User.Identity.IsAuthenticated)
                    {
                        <li>
                            <a asp-page="/Events/Index" class="sidebar-link flex items-center p-3 text-gray-700 rounded-lg hover:bg-blue-100/50 transition-all duration-200 group" data-page="/Events">
                                <svg class="w-4 h-4 text-gray-600 transition duration-200" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="currentColor" viewBox="0 0 20 20">
                                    <path d="M20 4a2 2 0 0 0-2-2h-2V1a1 1 0 0 0-2 0v1h-3V1a1 1 0 0 0-2 0v1H6V1a1 1 0 0 0-2 0v1H2a2 2 0 0 0-2 2v2h20V4Zm0 4H0v8a2 2 0 0 0 2 2h16a2 2 0 0 0 2-2V8Z"/>
                                </svg>
                                <span class="ml-3 font-medium">Événements</span>
                            </a>
                        </li>
                        @if (User.IsInRole("SysAdmin"))
                        {
                            <li>
                                <a asp-page="/Categories/Index" class="sidebar-link flex items-center p-3 text-gray-700 rounded-lg hover:bg-blue-100/50 transition-all duration-200 group" data-page="/Categories">
                                    <svg class="w-5 h-5 text-gray-600 transition duration-200" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 7h.01M7 3h5c.512 0 1.024.195 1.414.586l7 7a2 2 0 010 2.828l-7 7a2 2 0 01-2.828 0l-7-7A1.994 1.994 0 013 12V7a4 4 0 014-4z"/>
                                    </svg>
                                    <span class="ml-3 font-medium">Catégories</span>
                                </a>
                            </li>
                            <li>
                                <a asp-page="/Venues/Index" class="sidebar-link flex items-center p-3 text-gray-700 rounded-lg hover:bg-blue-100/50 transition-all duration-200 group" data-page="/Venues">
                                    <svg class="w-5 h-5 text-gray-600 transition duration-200" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"/>
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"/>
                                    </svg>
                                    <span class="ml-3 font-medium">Lieux</span>
                                </a>
                            </li>
                        }
                        <li>
                            <a asp-page="/Reservations/Index" class="sidebar-link flex items-center p-3 text-gray-700 rounded-lg hover:bg-blue-100/50 transition-all duration-200 group" data-page="/Reservations">
                                <svg class="w-5 h-5 text-gray-600 transition duration-200" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 11V7a4 4 0 00-8 0v4M5 9h14l1 12H4L5 9z"/>
                                </svg>
                                <span class="ml-3 font-medium">Réservations</span>
                            </a>
                        </li>
                    }
                    <li>
                        <a asp-page="/Privacy" class="sidebar-link flex items-center p-3 text-gray-700 rounded-lg hover:bg-blue-100/50 transition-all duration-200 group" data-page="/Privacy">
                            <svg class="w-5 h-5 text-gray-600 transition duration-200" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="currentColor" viewBox="0 0 20 20">
                                <path d="M10 .5a9.5 9.5 0 1 0 9.5 9.5A9.51 9.51 0 0 0 10 .5ZM9.5 4a1.5 1.5 0 1 1 0 3 1.5 1.5 0 0 1 0-3ZM12 15H8a1 1 0 0 1 0-2h1v-3H8a1 1 0 0 1 0-2h2a1 1 0 0 1 1 1v4h1a1 1 0 0 1 0 2Z"/>
                            </svg>
                            <span class="ml-3 font-medium">Confidentialité</span>
                        </a>
                    </li>
                </ul>
            </div>
        </div>
    </aside>

    <div class="lg:ml-64 min-h-screen flex flex-col">
        <!-- Top Header -->
        <header class="bg-white border-b border-gray-200 shadow-sm">
            <div class="px-4 py-4 lg:px-6 lg:py-4">
                <div class="flex items-center justify-between">
                    <div class="flex items-center flex-1">
                        <button data-drawer-target="sidebar" data-drawer-toggle="sidebar" aria-controls="sidebar" type="button" class="inline-flex items-center p-2 text-sm text-gray-600 rounded-lg lg:hidden hover:bg-gray-100 focus:outline-none focus:ring-2 focus:ring-blue-500 mr-3">
                            <span class="sr-only">Open sidebar</span>
                            <svg class="w-6 h-6" aria-hidden="true" fill="currentColor" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg">
                                <path clip-rule="evenodd" fill-rule="evenodd" d="M2 4.75A.75.75 0 012.75 4h14.5a.75.75 0 010 1.5H2.75A.75.75 0 012 4.75zm0 10.5a.75.75 0 01.75-.75h7.5a.75.75 0 010 1.5h-7.5a.75.75 0 01-.75-.75zM2 10a.75.75 0 01.75-.75h14.5a.75.75 0 010 1.5H2.75A.75.75 0 012 10z"></path>
                            </svg>
                        </button>
                        <!-- Search Bar -->
                        <div class="flex-1 max-w-md">
                            <div class="relative">
                                <div class="absolute inset-y-0 left-0 flex items-center pl-3 pointer-events-none">
                                    <svg class="w-5 h-5 text-gray-400" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 20 20">
                                        <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m19 19-4-4m0-7A7 7 0 1 1 1 8a7 7 0 0 1 14 0Z"/>
                                    </svg>
                                </div>
                                <input type="text" id="search-navbar" class="block w-full p-2.5 pl-10 text-sm text-gray-900 border border-gray-300 rounded-lg bg-gray-50 focus:ring-blue-500 focus:border-blue-500" placeholder="Rechercher...">
                            </div>
                        </div>
                    </div>
                    <!-- Right side icons and user -->
                    <div class="flex items-center space-x-4 ml-4">
                        @if (User.Identity.IsAuthenticated)
                        {
                            var currentUser = await UserManager.GetUserAsync(User);
                            var displayName = currentUser?.FullName ?? User.Identity.Name;
                            <!-- User Profile -->
                            <div class="flex items-center space-x-6">
                                <div class="flex items-center space-x-2">
                                    <div class="w-8 h-8 bg-blue-500 rounded-full flex items-center justify-center text-white font-semibold text-sm">
                                        <span>@(displayName?[0].ToString().ToUpper() ?? "U")</span>
                                    </div>
                                    <div class="hidden md:block text-left">
                                        <p class="text-sm font-medium text-gray-900">@displayName</p>
                                        <p class="text-xs text-gray-500">@(User.IsInRole("SysAdmin") ? "Admin" : "Organizer")</p>
                                    </div>
                                </div>
                                <span class="py-1.5 px-2.5 bg-red-100 rounded-lg text-sm text-red-600 hover:text-red-700 font-medium ml-2 cursor-pointer" id="logout-trigger">
                                    <div class="flex items-center gap-2">
                                        <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-log-out-icon lucide-log-out"><path d="m16 17 5-5-5-5"/><path d="M21 12H9"/><path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"/></svg>
                                        Se déconnecter
                                    </div>
                                </span>
                            </div>
                        }
                        else
                        {
                            <a asp-page="/Account/Login" class="text-sm text-blue-600 hover:text-blue-800 font-medium">Login</a>
                        }
                    </div>
                </div>
            </div>
            
            <!-- Tabs Navigation -->
            <div class="border-t border-gray-200 bg-white">
                <div class="px-4 lg:px-6">
                    <ul class="flex flex-wrap -mb-px text-sm font-medium text-center">
                        <li class="me-2">
                            <a href="/" class="tab-link inline-flex items-center justify-center p-4 border-b-2 border-transparent rounded-t-lg text-gray-600 hover:text-blue-600 hover:border-blue-300 transition-all duration-200" data-page="/">
                                Tableau de bord
                            </a>
                        </li>
                        @if (User.Identity.IsAuthenticated)
                        {
                            <li class="me-2">
                                <a asp-page="/Events/Index" class="tab-link inline-flex items-center justify-center p-4 border-b-2 border-transparent rounded-t-lg text-gray-600 hover:text-blue-600 hover:border-blue-300 transition-all duration-200" data-page="/Events">
                                    Événements
                                </a>
                            </li>
                            @if (User.IsInRole("SysAdmin"))
                            {
                                <li class="me-2">
                                    <a asp-page="/Categories/Index" class="tab-link inline-flex items-center justify-center p-4 border-b-2 border-transparent rounded-t-lg text-gray-600 hover:text-blue-600 hover:border-blue-300 transition-all duration-200" data-page="/Categories">
                                        Catégories
                                    </a>
                                </li>
                                <li class="me-2">
                                    <a asp-page="/Venues/Index" class="tab-link inline-flex items-center justify-center p-4 border-b-2 border-transparent rounded-t-lg text-gray-600 hover:text-blue-600 hover:border-blue-300 transition-all duration-200" data-page="/Venues">
                                        Lieux
                                    </a>
                                </li>
                            }
                            <li class="me-2">
                                <a asp-page="/Reservations/Index" class="tab-link inline-flex items-center justify-center p-4 border-b-2 border-transparent rounded-t-lg text-gray-600 hover:text-blue-600 hover:border-blue-300 transition-all duration-200" data-page="/Reservations">
                                    Commandes
                                </a>
                            </li>
                        }
                        <li class="me-2">
                            <a asp-page="/Privacy" class="tab-link inline-flex items-center justify-center p-4 border-b-2 border-transparent rounded-t-lg text-gray-600 hover:text-blue-600 hover:border-blue-300 transition-all duration-200" data-page="/Privacy">
                                Confidentialité
                            </a>
                        </li>
                    </ul>
                </div>
            </div>
        </header>

        <!-- Main Content -->
        <main class="flex-1 p-4 lg:p-6">
            <div class="mx-auto h-full">
                @RenderBody()
            </div>
        </main>

        <!-- Footer -->
        <footer class="bg-white border-t border-gray-200 mt-auto">
            <div class="px-4 py-3 lg:px-6">
                <div class="flex items-center justify-center text-sm text-gray-500">
                    &copy; 2025 - Ticketing Admin - <a asp-page="/Privacy" class="ml-1 text-blue-600 hover:text-blue-700 hover:underline transition-colors">Confidentialité</a>
                </div>
            </div>
        </footer>
    </div>

    <!-- Global Logout Confirmation Modal -->
    <div id="logout-modal" class="fixed inset-0 z-[100] hidden items-center justify-center bg-gray-900 bg-opacity-50 transition-opacity duration-300">
        <div class="bg-white rounded-xl shadow-2xl p-6 w-full max-w-md m-4 transform transition-all duration-300 scale-95 opacity-0">
            <div class="flex items-center gap-3 mb-4 text-red-600 border-b border-gray-100 pb-4">
                <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-log-out"><path d="m16 17 5-5-5-5"/><path d="M21 12H9"/><path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"/></svg>
                <h3 class="text-lg font-bold text-gray-900">Confirmation de déconnexion</h3>
            </div>
            <p class="text-gray-600 mb-6">
                Êtes-vous sûr de vouloir vous déconnecter de votre session ?
            </p>
            
            <div class="flex justify-end space-x-3">
                <button id="logout-cancel" type="button" class="px-4 py-2 text-sm font-medium text-gray-700 bg-gray-100 rounded-lg hover:bg-gray-200 transition">
                    Annuler
                </button>
                <a href="/Account/Logout" class="bg-red-600 hover:bg-red-700 text-white px-4 py-2 text-sm font-medium rounded-lg transition shadow-lg shadow-red-100">
                    Se déconnecter
                </a>
            </div>
        </div>
    </div>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/flowbite/2.2.1/flowbite.min.js"></script>

    <script>
        // Set active states for sidebar and tabs based on current page
        document.addEventListener('DOMContentLoaded', function() {
            const currentPath = window.location.pathname.toLowerCase();
            
            // Handle sidebar links
            document.querySelectorAll('.sidebar-link').forEach(link => {
                const pagePath = link.getAttribute('data-page').toLowerCase();
                if (currentPath === pagePath || 
                    (pagePath === '/' && (currentPath === '' || currentPath === '/index')) ||
                    (pagePath === '/events' && currentPath.startsWith('/events')) ||
                    (pagePath === '/venues' && currentPath.startsWith('/venues'))) {
                    link.classList.add('active-sidebar-item');
                    link.classList.remove('text-gray-700', 'hover:bg-blue-100/50');
                    const svg = link.querySelector('svg');
                    if (svg) {
                        svg.classList.remove('text-gray-600');
                        svg.classList.add('text-blue-600');
                    }
                }
            });
            
            // Handle tab links
            document.querySelectorAll('.tab-link').forEach(link => {
                const pagePath = link.getAttribute('data-page').toLowerCase();
                if (currentPath === pagePath || 
                    (pagePath === '/' && (currentPath === '' || currentPath === '/index')) ||
                    (pagePath === '/events' && currentPath.startsWith('/events')) ||
                    (pagePath === '/venues' && currentPath.startsWith('/venues'))) {
                    link.classList.remove('border-transparent', 'text-gray-600');
                    link.classList.add('border-blue-600', 'text-blue-600', 'font-semibold', 'active');
                }
            });

            // Logout Modal Logic
            const logoutTrigger = document.getElementById('logout-trigger');
            const logoutModal = document.getElementById('logout-modal');
            const logoutCancel = document.getElementById('logout-cancel');
            const logoutDialog = logoutModal ? logoutModal.querySelector('div:first-child') : null;

            if (logoutTrigger && logoutModal) {
                function showLogoutModal() {
                    logoutModal.classList.remove('hidden');
                    logoutModal.classList.add('flex');
                    setTimeout(() => {
                        logoutModal.classList.add('opacity-100');
                        if (logoutDialog) logoutDialog.classList.add('scale-100', 'opacity-100');
                    }, 10);
                }

                function hideLogoutModal() {
                    if (logoutDialog) logoutDialog.classList.remove('scale-100', 'opacity-100');
                    logoutModal.classList.remove('opacity-100');
                    setTimeout(() => {
                        logoutModal.classList.remove('flex');
                        logoutModal.classList.add('hidden');
                    }, 300);
                }

                logoutTrigger.addEventListener('click', function(e) {
                    e.preventDefault();
                    showLogoutModal();
                });

                if (logoutCancel) {
                    logoutCancel.addEventListener('click', hideLogoutModal);
                }

                logoutModal.addEventListener('click', function(e) {
                    if (e.target === logoutModal) {
                        hideLogoutModal();
                    }
                });
            }
        });
    </script>

    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>